version: "3"
services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ai_notes
    ports:
      - "3307:3306"
  app:
    build: .
    env_file: .env  
    ports:
      - "8501:8501"
    depends_on:
      - mysql
